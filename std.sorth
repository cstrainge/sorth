
( The standard library of words for Sorth. )


( Lets save some typing. )
: .cr . cr ;


( Simple increment and decrements. )
: ++ 1 + ;
: -- 1 - ;


( Values for true and false. )
: true  1 ;
: false 0 ;


( For testing ifs... )
: hello if "Hello world!" else "Goodbye all." then .cr ;

( Testing loops... )
: loop_until 10 begin "Hello world." .cr -- dup 0 <= until ;
: loop_while 10 begin -- dup 0 >= while "Hello world." .cr repeat ;


(
Missing functionality

  swap
  over
)
